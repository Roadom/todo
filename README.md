# Todo CLI (Go + Huh)

A simple terminal-based todo list application built in Go using the `huh` TUI library. Tasks are persisted using a local JSON file.

---

## Features

- View all tasks in a selectable list
- Add new tasks
- Rename existing tasks
- Toggle completion status
- Delete tasks (with confirmation)
- Persistent storage via `todos.json`
